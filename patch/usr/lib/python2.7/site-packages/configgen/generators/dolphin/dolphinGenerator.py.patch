##
## Executa um script e não o emulador.
## O script cria configurações adicionais para o dolphin.
##
--- dolphinGenerator.py.original
+++ dolphinGenerator.py.modificado
@@ -207,7 +207,7 @@
         except Exception:
             pass # don't fail in case of SYSCONF update
 
-        commandArray = ["dolphin-emu", "-e", rom]
+        commandArray = ["dolphin-emu.sh", "-e", rom]
         if system.isOptSet('platform'):
             commandArray = ["dolphin-emu-nogui", "-p", system.config["platform"], "-e", rom]
 
