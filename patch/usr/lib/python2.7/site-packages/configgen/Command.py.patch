#
# Corrige problemas com controles que usam nomes com caracteres especiais,
# como é o caso do adaptador de controle da oficial da sony para PS4
#
# Com a atualização do python 2 para o 3 talvês este patch não seja mais útil
# em uma atualização do batocera.plus
#
# Principais sistema standalone afetados: amigacd32 (fsuae), dreamcast (flycast)
#
--- Command.py.original
+++ Command.py.modificado
@@ -14,4 +14,4 @@
         for value in self.array:
             str.append(value)
 
-        return " ".join(str)
+        return " ".join(str).encode('utf-8')
